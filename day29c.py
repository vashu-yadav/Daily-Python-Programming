# Program using update()

employee = {
    "id": 101,
    "name": "Rahul",
    "salary": 25000
}

# Updating dictionary
employee.update({"salary": 30000})
employee.update({"department": "IT"})

print(employee)
